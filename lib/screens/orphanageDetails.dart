import 'package:flutter/material.dart';

class OrphanageDetails extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Orphanage Details Screen'),
    );
  }
}
